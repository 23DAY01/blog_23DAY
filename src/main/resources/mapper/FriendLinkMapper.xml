<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.day.blog.mapper.FriendLinkMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="site.day.blog.pojo.domain.FriendLink">
        <id column="id" property="id" />
        <result column="link_name" property="linkName" />
        <result column="link_avatar" property="linkAvatar" />
        <result column="link_address" property="linkAddress" />
        <result column="link_info" property="linkInfo" />
        <result column="deleted" property="deleted" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, link_name, link_avatar, link_address, link_info, deleted, create_time, update_time
    </sql>

</mapper>
